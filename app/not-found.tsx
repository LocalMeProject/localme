import NotFoundClient from "./not-found-client";

export default function NotFound() {
  // The client component renders the styled 404; metadata comes from the root layout.
  return <NotFoundClient />;
}
